<template>

</template>

<script lang="ts">
/*
    import { useStore } from 'vuex';
    import Cookies from 'js-cookie';

    const store = useStore();
    const userId = Cookies.get('userId');

    if (userId || store.getters('userId')) {
        const signOut = await UserService.signOut(userId);

    }
        */
export default {};
</script>